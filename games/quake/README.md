# Quake cabinet

Chrome lives here. `quake.{js,wasm}` is compiled from [njm-cursor-x/quake](https://github.com/njm-cursor-x/quake) at package time. `data/quake106.zip` is fetched then, never committed. Browser extracts `pak0.pak` via `vendor/lha.js`.
